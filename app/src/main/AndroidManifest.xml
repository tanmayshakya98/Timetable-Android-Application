<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.minorproject">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".findfreeroomfaculty"></activity>
        <activity android:name=".insertroom" />
        <activity android:name=".registernewuser" />
        <activity android:name=".facultyfreeslots" />
        <activity android:name=".dayselector" />
        <activity android:name=".facultyviewtimetable" />
        <activity android:name=".delete" />
        <activity android:name=".updateadmin" />
        <activity android:name=".viewingitems" />
        <activity android:name=".viewadmin" />
        <activity android:name=".insert" />
        <activity android:name=".facultyview" />
        <activity android:name=".selector" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>